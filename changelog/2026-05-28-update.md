---
title: 2026-05-28 跨日同步 — hermes-agent 963d22c → 11d9309（90 commits, v0.15.0 发布）
created: 2026-05-28
updated: 2026-05-28
type: changelog
tags: [release, anthropic, model-routing, memory-provider, hindsight, context-engine, kanban, sqlite, skills, web-tools, mcp, mtls, docker, image-gen, krea, xai, security, dashboard, discord]
sources:
  - hermes-agent/main 963d22c..11d9309
---

# 2026-05-28 跨日同步：hermes-agent main 963d22c → 11d9309（v0.15.0 发布）

> **基线**：wiki HEAD `66e5d25`（对齐 hermes `963d22c`，2026-05-27 13:55 -0700）
> **目标**：hermes-agent `main` HEAD `11d93096`（merge `#34097`，2026-05-28 13:56 -0700）
> **跨度**：90 个 commit（52 fix / 11 feat / 8 test / 8 chore / 6 docs / 1 refactor / 1 perf / 1 ci），窗口约 24 小时
> **版本**：**`pyproject.toml:version = "0.15.0"`**（自 v0.14.0 起首次 minor 升级；commit `0c859a1c0` "chore: release v0.15.0 (2026.5.28)"）
> **验证基线**：`/tmp/hermes-agent` clone @ `11d93096`，所有 file:line 引用为现行 source 实际行号；每条结论至少一条 `grep -n` / `git show` 命中

---

## 章节索引

1. [v0.15.0 "The Velocity Release" 正式发布](#1-v0150-the-velocity-release-正式发布)
2. [claude-opus-4.8 / claude-opus-4.8-fast 模型接入](#2-claude-opus-48--claude-opus-48-fast-模型接入)
3. [Memory Provider 主机契约扩展 — 完整回合消息 + Hindsight recall_types](#3-memory-provider-主机契约扩展)
4. [Context Engine 外部引擎主机契约](#4-context-engine-外部引擎主机契约)
5. [Kanban SQLite 健壮性 wave + UI 修复](#5-kanban-sqlite-健壮性-wave--ui-修复)
6. [Skills 目录全量拉取 — skills.sh sitemap + ClawHub 分页](#6-skills-目录全量拉取)
7. [web_crawl 工具移除 + provider crawl plumbing 清理](#7-web_crawl-工具移除)
8. [MCP mTLS 客户端证书支持](#8-mcp-mtls-客户端证书支持)
9. [Docker 镜像 wave](#9-docker-镜像-wave)
10. [Dashboard / Web UI 修复](#10-dashboard--web-ui-修复)
11. [Auth / Nous Portal entitlement + API_SERVER_KEY 强制](#11-auth--nous-portal-entitlement--api_server_key-强制)
12. [xAI 修复簇 — proxy 429 / OAuth bare-code paste / timeout fallback](#12-xai-修复簇)
13. [Agent / Provider 健壮性修复簇](#13-agent--provider-健壮性修复簇)
14. [安全加固 — tar member 拒绝 / msgraph CIDR / npm overrides](#14-安全加固)
15. [Image Gen — Krea 2 Medium/Large 入 FAL 目录](#15-image-gen--krea-2-mediumlarge-入-fal-目录)
16. [CLI / update / patch / Discord / TUI 细粒度修复](#16-cli--update--patch--discord--tui-细粒度修复)
17. [Wiki 同步：文件矩阵 + 验证基线](#17-wiki-同步文件矩阵--验证基线)

---

## 1. v0.15.0 "The Velocity Release" 正式发布

本次同步窗口内最重磅的事件：hermes-agent 自 v0.14.0 以来首次正式 minor 升级，**v0.15.0（v2026.5.28）"The Velocity Release"** 正式打标。

`0c859a1c0` "chore: release v0.15.0 (2026.5.28) (#34008)" 改动 7 文件：

| 文件 | 改动 |
|------|------|
| `pyproject.toml` | `version = "0.15.0"`（已验证现行值） |
| `hermes_cli/__init__.py` | `__version__ = "0.15.0"`（已验证现行值） |
| `acp_registry/agent.json` | bump 到 `0.15.0`（与 pyproject 同步） |
| `RELEASE_v0.15.0.md` | **新增 655 行**发布说明 |
| `scripts/release.py` | +18 行 |
| `scripts/contributor_audit.py` | +3 行 |
| `uv.lock` | +2 行 |

`RELEASE_v0.15.0.md:3` 自报统计（**自 v0.14.0 累计**，非本日 delta）：

> **Since v0.14.0:** 1,302 commits · 747 merged PRs · 1,746 files changed · 282,712 insertions · 36,699 deletions · 560+ issues closed (15 P0, 65 P1, 19 security-tagged) · 321 community contributors

⚠️ **重要边界**：`RELEASE_v0.15.0.md` 描述的是 v0.14.0 → v0.15.0 的**累计**变更，其绝大多数 highlight（promptware 防御、Bitwarden、Honcho、Dashboard OAuth、Krea 原生插件、ntfy、Curator、session_search 重建、run_agent.py 16k→3.8k 重构等）**已在 wiki 既往日同步页（2026-05-22 ~ 2026-05-27）中逐条覆盖**。本页只记录 `963d22c..11d9309` 这 90 个**新** commit 中尚未文档化的内容。

发布说明命名的几个累计主题（供索引，非本次新增 — 详见对应专页）：
- **The Big Refactor**：`run_agent.py` 16,083 → 3,821 行（-76%），拆为 14 个 `agent/*` 模块（#27248）
- **Kanban 成长为多 Agent 平台**：104 PR（见 [[kanban-multi-agent-board]]）
- **冷启动性能 wave**：又减 ~1s，每会话函数调用 -47%（见 [[2026-05-27-update]] §12）
- **session_search 重建**：去 LLM、免费、4,500× 提速（#27590）
- **Promptware 防御**：见 [[security-defense-system]]（#32269/#33131）

配套文档：
- `b1d3ead7f` "docs: tweak v0.15.0 release notes (#34037)"
- `8b6beaab5` "docs: 30-day overhaul — correctness audit, PR coverage, Nous Portal weave, sidebar reorg (#33782)" — 文档站 30 天大检修

---

## 2. claude-opus-4.8 / claude-opus-4.8-fast 模型接入

`1a7479573` "feat: add claude-opus-4.8 and claude-opus-4.8-fast (#34003)" — Anthropic 于 2026-05-27 发布 Claude Opus 4.8，覆盖 OpenRouter / Anthropic / Bedrock / Claude Platform on AWS。

三处源码改动，**全部现行可验证**：

| 文件:行 | 内容 |
|---------|------|
| `hermes_cli/models.py:35-36` | OpenRouter fallback 快照新增 `("anthropic/claude-opus-4.8", "")` + `("anthropic/claude-opus-4.8-fast", "2x price, higher output speed")` |
| `hermes_cli/models.py:144` | Nous Portal curated list 加 `"anthropic/claude-opus-4.8"` |
| `hermes_cli/models.py:296` | Anthropic-native picker list 加 `"claude-opus-4-8"` |
| `agent/model_metadata.py:144-145` | `DEFAULT_CONTEXT_LENGTHS`：`"claude-opus-4-8": 1000000` + `"claude-opus-4.8": 1000000`（1M tokens，与 4.6/4.7 一致） |
| `agent/model_metadata.py:98` | 最大输出 `"claude-opus-4-8": 128_000` |
| `agent/anthropic_adapter.py:80` | `_XHIGH_EFFORT_SUBSTRINGS = ("4-7", "4.7", "4-8", "4.8")` |
| `agent/anthropic_adapter.py:85` | `_ADAPTIVE_THINKING_SUBSTRINGS` 加 `"4-8"/"4.8"` |
| `agent/anthropic_adapter.py:89` | `_NO_SAMPLING_PARAMS_SUBSTRINGS = ("4-7", "4.7", "4-8", "4.8")` |

**关键语义**（commit body）：
- `claude-opus-4.8-fast` 是**独立 model ID**，定价为基础模型 **2×**（旧 Opus 4.6/4.7 fast 溢价为 6×，显著改善）。
- 它**不是** `speed: "fast"` 请求参数（那是 Opus 4.6 的 native fast-mode beta 路径）；4.8 继承 Opus 4.7 的 API 契约：仅 adaptive thinking、支持 xhigh effort、采样参数（temperature/top_p/top_k）返回 400。
- live catalog 可达时自动 surface 新模型；fallback 快照仅在 manifest 拉取失败时生效。

> 详见 [[smart-model-routing]]（已同步新增 Opus 4.8 行）。

---

## 3. Memory Provider 主机契约扩展

两个 feat 让外部/社区 memory plugin 拿到更丰富的回合上下文。

### 3.1 完整回合消息暴露给 memory provider

`5a95fb2e1` "feat: expose completed-turn message context to memory providers" — 给 `MemoryProvider.sync_turn` 契约加可选 `messages` 关键字，外部 plugin 可收到 OpenAI 风格的完整对话消息列表（含 assistant 工具调用 + tool result content），不再只有最终 assistant 文本。

- **签名内省派发**：`_provider_sync_accepts_messages` 只给声明了 `messages` 形参（或 `**kwargs`）的 provider 传该参数；所有现有 in-tree provider 保持 legacy 纯文本签名，调用不变。
- 不在 core 加结构化 trace 信封 — provider 自行从标准消息列表重建所需内容。
- 改动文件：`agent/conversation_loop.py` / `agent/memory_manager.py`（+35）/ `agent/memory_provider.py`（+13）/ `run_agent.py`，并把 **Memori** 文档化为独立社区 memory provider。
- 自 #28065 salvage 重 rebase；co-author Dave Heritage（memorilabs.ai）。

> merge HEAD `11d93096` 标题即 "Merge pull request #34097 ... salvage/memori-trace-messages"。

### 3.2 Hindsight recall_types 默认 observation-only

`490b3e76b` "feat(hindsight): default recall_types to observation only" — 自动召回过去把 Hindsight 的三类 fact（`world` / `experience` / `observation`）全部上屏，三种 framing 重复同一信号、烧掉 `recall_max_tokens`。

现行源码（`plugins/memory/hindsight/__init__.py`）：

| 行 | 内容 |
|----|------|
| `:590` | `self._recall_types: list[str] = ["observation"]`（默认值，原为 `None` = 服务端"全返回"） |
| `:867` | `get_config_schema()` 加 `recall_types` 键（`default: "observation"`，可发现于 `hermes config`） |
| `:1202-1209` | `initialize()` 解析 config：缺失→`["observation"]`；逗号分隔字符串→split（与 `recall_tags` 一致）；显式 `recall_types=[]`→回退默认（不静默放宽） |
| `:1346-1347` | 召回时 `if self._recall_types: recall_kwargs["types"] = self._recall_types` |

- 配套 `4df62d239` "docs(hindsight): correct recall_types scope — tool path is also narrowed"。
- 逐次 `hindsight_recall` 工具调用不受影响（仅当 caller 显式传 `types` 才转发）。

> 详见 [[memory-system-architecture]]（已同步）。

---

## 4. Context Engine 外部引擎主机契约

`9b5dae17a` "feat(context-engine): host contract for external context engines" — 把 5 个旧 PR（#16453/#17453/#16451/#17600/#13373）凝练成一个最小通用主机契约，供外部 context engine plugin（如 hermes-lcm）干净集成；删掉重复或边际价值低的脚手架。`RELEASE_v0.15.0.md` 记为 #33750。

五处具体改动，已验证现行源码：

1. **`_transition_context_engine_session()` on AIAgent**（`run_agent.py:530`）— 通用生命周期助手，依次触发 `on_session_end → on_session_reset → on_session_start → 可选 carry_over_new_session_context`；engine 只实现需要的钩子，缺失钩子跳过。`reset_session_state()` 现可选接收 `previous_messages` / `old_session_id` / `carry_over_context`，有元数据时委托给该 transition 助手（`run_agent.py:621,643`）。内置 compressor 保持原 reset-only 行为。
2. **`conversation_id` 传入 `on_session_start()`** — agent-init（`agent/agent_init.py:1519`）+ compression-boundary（`agent/conversation_compression.py:419-420`）两处都转发 `self._gateway_session_key`，使 plugin engine 拥有跨 session_id 轮换（压缩切分 / `/new` / resume）稳定的会话身份。
3. **规范化 cache 桶转发给 engine** — `update_from_response()` 的 usage dict 现含 `input_tokens / output_tokens / cache_read_tokens / cache_write_tokens / reasoning_tokens`（叠加在 legacy prompt/completion/total 之上），engine 可基于 cache 命中率与 reasoning 成本决策。
4. ABC 文档更新（`agent/context_engine.py:135` `on_session_start(self, session_id, **kwargs)`）。
5. 回归覆盖：`tests/agent/test_context_engine_host_contract.py`（驱动完整生命周期）。

配套 `8595281f3` "fix: expose context engine tools with saved toolsets" — context engine 注入的工具在 saved toolset 路径下也能暴露。

> 相关 [[context-compressor-architecture]]、[[agent-loop-and-prompt-assembly]]。

---

## 5. Kanban SQLite 健壮性 wave + UI 修复

本次最密集的修复簇（~14 commit），把 kanban 的 SQLite 持久层从"生产环境第 6 次损坏"打到可检测/可恢复。全部落在 `hermes_cli/kanban_db.py`，现行行号已验证。

### 5.1 抗 torn-write 损坏（核心）

`6416dd518` "harden SQLite against torn-write corruption (secure_delete + cell_size_check + synchronous=FULL)" — 生产 corruption #6 是 Bug E（b-tree 页头清零、旧 cell 内容残留）。每次 `connect()` 应用三条 pragma：

| pragma | 行 | 作用 |
|--------|----|----|
| `PRAGMA synchronous=FULL`（原 NORMAL） | `kanban_db.py:1305` | 关闭 WAL-checkpoint 重排窗口；本地 SSD <1ms/commit |
| `PRAGMA secure_delete=ON` | `:1310` | 释放页字节清零，损坏可检测、旧行不会复活为活数据；首连写持久标志入 header |
| `PRAGMA cell_size_check=ON` | `:1313` | 读侧守卫，坏 cell 在读时报错而非静默返回错数据 |

### 5.2 其余 13 个健壮性/UI 修复

| commit | 主题 | 验证锚点 |
|--------|------|---------|
| `5c49cd0ed` | `fix(state)`：transient EIO 下永不静默把 WAL 降级为 DELETE | — |
| `99c19eb2f` | write_txn 加 post-commit `page_count` 不变量检查 | `kanban_db.py:1633,1697`（header page_count vs 实际文件页） |
| `e83252dc4` | write_txn rollback 失败时保留原始异常 | — |
| `c002668ff` | `detect_crashed_workers` 加 grace period | `:139,147` `_resolve_crash_grace_seconds` |
| `ffdc937c1` | zombie reaper 从 `dispatch_once` 提出（hoist） | — |
| `dc98314fb` | 已是 WAL 的连接跳过冗余 WAL pragma | `:1260` |
| `ebe04c66c` | 长生命周期进程每次 `connect()` 后关闭 kanban.db FD | — |
| `90b6b3d18` | 加固 sqlite 连接并发 | — |
| `3ba896273` | Windows 初始化锁守卫 | `:1023` `_cross_process_init_lock`（跨进程串行化首连 WAL/schema/integrity） |
| `6f9182cb3` | 损坏 DB 备份用 content-addressed 文件名 | `:1142` `Copy a corrupt DB ... to a content-addressed backup` |
| `f30db14ce` | worker 收到 SIGTERM 必须终止进程（#28181） | — |
| `70abae8e3` | 列改横向滚动条而非换行 | TUI/web |
| `538f0fa33` | 列折行成多行 + 修垂直溢出 | TUI/web |

配套测试：`2d5dcfabc`（dispatcher tick counter 对齐 hoisted zombie reaper）、`36c99af37`（两测试对齐 kanban 加固）。

> 详见 [[kanban-multi-agent-board]]（已同步新增"SQLite 抗损坏加固"节）。

---

## 6. Skills 目录全量拉取

两个 source 此前都因分页/抓取限制只暴露目录极小一部分，本次修为全量。

### 6.1 skills.sh 走 sitemap（858 → 19,932）

`7050c052e` "fix(skills): pull full skills.sh catalog via sitemap (858 → 19,932) (#34025)" — 旧 `SkillsShSource` 跑 28 个硬编码关键词搜索（各 cap 50），只得 ~858。真实目录 ~20k，经 `sitemap-skills-{1,2}.xml` 暴露。

现行源码 `tools/skills_hub.py`：

| 行 | 内容 |
|----|------|
| `:1215` | `class SkillsShSource(SkillSource)` |
| `:1224` | `SITEMAP_INDEX_URL = "https://www.skills.sh/sitemap.xml"` |
| `:1262` | 空查询路径改走 `self._sitemap_catalog(limit)` |
| `:1323` | `_sitemap_catalog()` — 走 sitemap 枚举全目录，sitemap 不可达时回退 `_featured_skills` |
| `:1342` | 处理 httpx + brotlicffi 交互：per-skill sitemap 用 `Accept-Encoding: gzip` |

`build_skills_index.crawl_skills_sh()` 现只调 `search("", limit=0)`（一次 HTTP 往返替代 28 次关键词搜索）。

### 6.2 ClawHub 全目录分页（200 → 20k+）

`fb9f3a4ef` "fix(skills): pull full ClawHub catalog into the skills index (200 → 20k+) (#33748)" — `ClawHubSource.search("")` 空查询走单次无分页请求，ClawHub API 单页 cap 200 并返回 `nextCursor`；旧逻辑抓第 1 页就停，缓存索引静默截断 99%。修为按 `nextCursor` 翻页拉全量。

### 6.3 其他 skills 修复

- `e0572a6de` "fix(skills-hub): stop ellipsis-truncating the Identifier column (#33810)" — 标识符列不再省略号截断。
- `a1eaad2fc` "perf(skills-page): lazy-fetch the catalog instead of bundling 34MB into JS (#33809)" — dashboard skills 页改懒加载目录，不再把 34MB 打进 JS bundle。

> 详见 [[skills-system-architecture]]（已同步）。

---

## 7. web_crawl 工具移除

`5e1f79343` "chore(web): remove web_crawl tool + provider crawl plumbing (#33824)" — `web_crawl_tool()` 是孤儿：无 model schema 注册、无 skill/CLI 调用、agent 无法调用。决定不把 crawl 暴露为独立能力（`web_search` + `web_extract` 已覆盖目标用例）。

**净改动 25 文件，+115/-1067**，移除项（已验证 `tools/web_tools.py` 现已无 `web_crawl`）：

| 移除位置 | 内容 |
|----------|------|
| `tools/web_tools.py` | `web_crawl_tool()`（~230 LOC） |
| `plugins/web/firecrawl/provider.py` | `supports_crawl()` + `crawl()` |
| `plugins/web/tavily/provider.py` | `supports_crawl()` + `crawl()` |
| `plugins/web/xai/provider.py` | `supports_crawl()` override |
| `agent/web_search_provider.py` | `supports_crawl()` + `crawl()` ABC 方法 |
| `agent/web_search_registry.py` | `get_active_crawl_provider()` + `_resolve()` 的 `'crawl'` 分支 |
| `agent/display.py` | web_crawl 工具进度渲染 |
| `hermes_cli/config.py` | `TAVILY_API_KEY.tools` 中的 `'web_crawl'` |
| 文档 | EN + zh-Hans capability 表删 Crawl 列 |

Closes #33762（schema-text bug 仅当 #32608 落地才存在）。

配套 `5f66c3647` "fix(redact): pass web URLs through unchanged (#34029)" — redaction 放行 web URL 不变形。

> 详见 [[web-tools-architecture]]（已同步：provider 能力表去 crawl 维度）。

---

## 8. MCP mTLS 客户端证书支持

`87e5b2fae` "feat(mcp): support TLS client certificates (mTLS) for HTTP and SSE servers (#33721)" — 加一等公民 `client_cert` / `client_key` 配置键，mTLS 后的 MCP server 无需外部 TLS 终止代理。回应社区提问（Jeremy W.）。

现行源码 `tools/mcp_tool.py:562` `_resolve_client_cert(server_name, config)`，schema（`mcp_servers.<name>`，仅 HTTP/SSE）：

- `client_cert: "/path/to/combined.pem"` — 单 PEM 含 cert+key
- `client_cert` + `client_key` — 分开两文件
- `client_cert: [cert, key]` 或 `[cert, key, password]` — list 形式，可选加密 key 口令（`:608-622`）
- 路径支持 `~` 展开（`_expand`）；缺文件在 connect 时抛 server-scoped `FileNotFoundError`（而非晚到的 opaque TLS 握手错误）

接线：
- 新 SDK HTTP 路径（mcp ≥ 1.24）：在 user-owned `httpx.AsyncClient` 上加 `cert=`，与既有 `verify=` 并列。
- SSE 路径：经 `httpx_client_factory` 包裹 SDK 默认值（`follow_redirects=True`）再叠 `verify` + `cert`；仅需要时注入，默认仍用 SDK 内置 `create_mcp_http_client`。
- 废弃的 mcp<1.24 路径不动（`streamablehttp_client` 签名不暴露 `cert`）。
- 顺带文档化此前未文档的 `ssl_verify` 键。
- 测试 `tests/tools/test_mcp_client_cert.py`。

> 详见 [[mcp-and-plugins]]（已同步新增"mTLS 客户端证书"节）。

---

## 9. Docker 镜像 wave

7 个 docker fix，聚焦容器内 CLI 行为与可观测性：

| commit | 主题 |
|--------|------|
| `66489f38c` | 把 build-time git SHA 烤进镜像 — `.dockerignore` 排除 `.git` 导致 `hermes dump` / 启动 banner 显示 `(unknown)`，无法从容器 bug 报告定位 commit |
| `b924b22a9` | `hermes update` 在 docker 内打印 `docker pull` 指引而非伪 git 错误（测试 `875d930ac` stub subprocess.run） |
| `c341a2d10` | 对齐 dashboard 与 s6 gateway 服务的 `HOME`（#33481） |
| `71b4a6b18` | 安装 `python-is-python3`，容器内裸 `python` 可解析 |
| `aeb992d34` | 调 CLI 前去掉 `docker exec` 到 hermes uid |
| `b34532319` | 把受监管 gateway stdout tee 到 docker logs |
| `0927fb558` | s6 镜像内 `gateway run` 自动重定向到受监管模式 |

> 相关 [[cli-architecture]]、[[messaging-gateway-architecture]]。

---

## 10. Dashboard / Web UI 修复

| commit | 主题 |
|--------|------|
| `fe5c8ec4a` | loopback 模式下 stale-token 401 自动重载 SPA |
| `c9410b346` | dashboard 可折叠侧边栏（#33421） |
| `10f13c388` | 允许移动端 dashboard 滚动（#28051/#28577） |

> 相关 [[dashboard-auth-oauth-gate]]、[[hook-system-architecture]]。

---

## 11. Auth / Nous Portal entitlement + API_SERVER_KEY 强制

- `406901b27` "feat(auth) normalise the way in which we check whether a user has free/paid access to nous portal" — 统一 free/paid Nous Portal 访问检查，便于按之 expose 行为与错误消息。
- `1cf5e639b` "fix(auth): refresh Nous entitlement in tool menus" + `dc52b82d5` "test(auth): update entitlement CI expectations"。
- `09a5cd808` "fix(auth): sync manual:device_code Codex pool entries on re-auth (#33744)" — 重新鉴权时同步 manual:device_code 的 Codex 凭据池条目。
- `1a9ef8314` "fix(security): require API_SERVER_KEY before dispatching API server work" — 现行 `gateway/platforms/api_server.py:4105-4121` `connect()` 在缺 `API_SERVER_KEY` 时拒绝启动 API server（不论绑定地址），并对空/占位 key 给出明确拒绝日志。

> 相关 [[messaging-gateway-architecture]]、[[security-defense-system]]、[[credential-pool-and-isolation]]。

---

## 12. xAI 修复簇

- `4ed482549` "fix(xai-proxy): handle 429 rate-limit responses in proxy retry path" + 回归测试 `c7f7783e5`（#28932）。
- `a82c88bac` "fix(xai-oauth): accept bare-code manual paste (state=None) (#26923)" — OAuth 手动粘贴 bare code（无 state）也接受。
- `442a9203c` "Fix xAI OAuth timeout manual fallback" — OAuth 超时走手动 fallback。

> 相关 [[hermes-proxy]]、[[credential-pool-and-isolation]]。

---

## 13. Agent / Provider 健壮性修复簇

| commit | 主题 | 验证锚点 |
|--------|------|---------|
| `0554ef1aa` | provider content-policy 拦截立即 fallback（#33883） | `agent/conversation_loop.py:3077,3093,3154,3185,3200`；`error_classifier.py:50` `content_policy_blocked` |
| `67011cc0d` | 缓冲 retry/fallback 状态，仅在终态失败时上屏（#33816） | 消除中途 "retrying..." 噪声 |
| `7a3c38d0b` | 无 provider 上下文上限时停止 probe stepdown | — |
| `b5495db70` | 跨 provider fallback 到 require-side provider 时重 pad `reasoning_content` | — |
| `9179396cb` | 仅当最终响应确认投递才置 `_final_content_delivered` | stream-consumer |
| `459d7694d` | 预加载 jiter native parser | — |
| `e8b9369a9` | openrouter 在 `extra_body` 传 `session_id` 做 sticky routing | `plugins/model-providers/openrouter/__init__.py:46-47`；grok 模型另加 `x-grok-conv-id` 头（`:93-94`） |
| `3a9bc9d88` | 统一 `/model` 与 `hermes model` 列表 + 加磁盘缓存（#33867） | model picker |
| `321ce94e2` | 非-minimax 溢出测试对齐新 keep-context 行为 | test |

> 相关 [[auxiliary-client-architecture]]、[[interrupt-and-fault-tolerance]]、[[smart-model-routing]]。

---

## 14. 安全加固

- `a91b1c8b3` "fix(tirith): reject non-regular tar members during auto-install process" — 自动安装解包时拒绝非常规 tar 成员（symlink/hardlink/device）；`tools/tirith_security.py`（+41/-12），测试 `tests/tools/test_tirith_security.py`（+85）以 `member.type = tarfile.SYMTYPE` 验证拒绝。
- `aa3466063` "fix(android): reject unsafe tar members in psutil compatibility installer" — Android psutil 兼容安装器同样拒绝不安全 tar 成员。
- `43abc51f6` "fix(security): require source CIDR allowlisting for public msgraph webhook binds" — 公网绑定的 msgraph webhook 强制 source CIDR 白名单；`gateway/platforms/msgraph_webhook.py:67`（`_parse_allowed_source_cidrs`）/`:100`/`:136`（`_source_allowlist_required_but_missing`）/`:287`（源 IP 校验），不配 CIDR 又非 loopback 即拒绝。
- `0bf9b867c` "fix(website): pin serialize-javascript and uuid via npm overrides" — 经 npm overrides 钉死传递依赖版本。

> 详见 [[security-defense-system]]（已同步新增"2026-05-28 解包与 webhook 加固"小节）。

---

## 15. Image Gen — Krea 2 Medium/Large 入 FAL 目录

`6d947e4d7` "feat(image_gen/fal): add Krea 2 Medium + Large to FAL catalog (#33506)" — fal 于 2026-05-27 day-0 作为官方 API 伙伴上 Krea 2。加两变体到 `FAL_MODELS` 目录（位于 `tools/image_generation_tool.py`，非 plugin __init__）：

| 行 | model_id | display | price |
|----|----------|---------|-------|
| `:327-328` | `fal-ai/krea/v2/medium/text-to-image` | Krea 2 Medium | $0.030（text）/ $0.035（style refs） |
| `:349-350` | `fal-ai/krea/v2/large/text-to-image` | Krea 2 Large | $0.060（text）/ $0.065（style refs） |

- 共享参数 schema：`aspect_ratio`（8 档）/ `creativity`（raw\|low\|medium\|high，默认 medium）/ `seed` / `image_style_references`（≤10）；**无** `num_inference_steps` / `guidance_scale` / `num_images`（Krea 2 不暴露，supports-set 过滤器会防御性剥离）。
- 这是 **FAL 路由变体**；2026-05-27 已落地的 native-Krea-API 插件（`plugins/image_gen/krea/`，PR #33236）仍可用（直接用 Krea key 计费）。两路径收敛到同一底层模型。
- 用户已通过 FAL 或 Nous Portal 订阅计费即可用，无需直接注册 Krea。

> 相关 [[mcp-and-plugins]]（plugin 化模式）、[[hook-system-architecture]]（`register_image_gen_provider`）。

---

## 16. CLI / update / patch / Discord / TUI 细粒度修复

**CLI / update：**
- `5cbc3fbdc` "fix(cli): /yolo in chat must enable session bypass, not just set env var" — chat 内 `/yolo` 必须启用 session bypass，不只是设环境变量。
- `031983bbf` "fix: limit pre-update state snapshots" — 限制更新前状态快照数量。
- `432a69175` "fix(update): stream + idle-kill `npm run build` so a stalled webui-build can't soft-brick the install (#33803)" — webui build 卡死不再 soft-brick 安装。
- `10ee4a729` "fix(gateway): drain on Windows `hermes gateway stop` so sessions survive restart (#33798)"。

**Patch 工具：**
- `78be45860` "fix(patch): widen new_string \t/\r unescape to all match strategies (#33733)" + `e9f3f2b34` "fix(tools): unescape common sequences in new_string when escape_normalized matches" — `new_string` 的 `\t`/`\r` 反转义扩展到所有匹配策略。详见 [[fuzzy-matching-engine]]。

**Discord：**
- `eafe11d45` "fix(gateway): backfill Discord thread context" → `68ddd6b33` "refactor(discord): inline backfill gate and document intent" → `b243afb68` "fix(discord): skip backfill for auto-created threads and update test fakes" — Discord 线程上下文回填三连。详见 [[messaging-gateway-architecture]]。

**TUI / Voice：**
- `912e6e227` "fix(tui): suppress mouse-residue leaks during Python launcher startup (#31213)"。
- `3ad46933d` "docs(voice): use `uv pip install faster-whisper` in STT install hints (#29800)"。详见 [[voice-mode-architecture]]。

**Chore / 其他：**
- `9b5dae17a` 见 §4；`6f9182cb3` 见 §5。
- AUTHOR_MAP / release 杂项：`d464d08a5`、`c5e496e1c`、`f8896dedc`、`247b24b49`、`bb0ac5ced`、`7b778db47`。
- `ea5a6c216` "ci(deploy): allow workflow_dispatch to also trigger Vercel deploy (#34081)"。
- `c0d04694e` "docs(email): clarify gateway vs Himalaya setup"；`986abb3cf` "docs: drop stale Kimi/DeepSeek vision example (#33736)"。
- `4e702fe2d`、`4a6f1863a`：CI 抗噪 / ci-unblocker 回归测试。

---

## 17. Wiki 同步：文件矩阵 + 验证基线

### 新增页面（1）
- `changelog/2026-05-28-update.md`（本页）

### 更新页面（concept）

| 页面 | 改动摘要 |
|------|---------|
| `concepts/smart-model-routing.md` | 新增 claude-opus-4.8 / -fast 行（context 1M / 输出 128k / 2× fast 定价 / Opus 4.7 API 契约继承）；frontmatter `updated` |
| `concepts/memory-system-architecture.md` | 新增"MemoryProvider.sync_turn 完整回合消息契约"+"Hindsight recall_types 默认 observation-only"两节；sources/updated |
| `concepts/kanban-multi-agent-board.md` | 新增"SQLite 抗损坏加固 wave（2026-05-28）"节（三 pragma + page_count 不变量 + init lock + FD 关闭 + content-addressed 备份 + grace period + zombie reaper）；updated |
| `concepts/skills-system-architecture.md` | 新增"Skills 目录全量拉取（2026-05-28）"节（skills.sh sitemap 858→19,932 + ClawHub 200→20k+ 分页 + 懒加载）；updated |
| `concepts/web-tools-architecture.md` | 移除 crawl 能力维度，新增"web_crawl 移除（2026-05-28）"说明；updated |
| `concepts/mcp-and-plugins.md` | 新增"mTLS 客户端证书"节（`_resolve_client_cert` + 三 schema 形式 + ssl_verify）；sources/updated |
| `concepts/context-compressor-architecture.md` | 新增"外部 Context Engine 主机契约"节（`_transition_context_engine_session` + conversation_id + cache 桶转发）；updated |
| `concepts/security-defense-system.md` | 新增"2026-05-28 解包与 webhook 加固"小节（tar member 拒绝 ×2 + msgraph CIDR + API_SERVER_KEY + npm overrides）；updated |

### README.md / index.md
- badge：版本 `v0.14.0 (963d22c)` → `v0.15.0 (11d9309)`；changelog 计数 31 → 32
- "最后更新" 2026-05-27 → 2026-05-28；跟踪 HEAD `963d22c` → `11d93096`
- 顶部 changelog 索引新增本页一行

### 验证基线
- `/tmp/hermes-agent` clone @ `11d93096`（2026-05-28 13:56 -0700）
- 每条结论至少一条 `grep -n` / `git show <sha>` 命中现行 source；所有 file:line 为现行实际行号
- 90 commit 中 11 个 feat + 安全 + perf 重点抓上游 PR body + 实际 hunk 核对
