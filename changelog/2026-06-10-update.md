# 2026-06-10 — hermes-agent `a5d05cf30 → a8f404b29`（99 commits 单日同步）

> **基线**：hermes-agent/main `a5d05cf30e8f5d79689ff8cee3ed806a6ca1c00e`（2026-06-09T16:47 -0400，*"fix(nix); don't run .#fix-lockfiles"*）→ `a8f404b29fa9ffde4ba4763e2bc30a077f430fa0`（2026-06-07 +0200 commit date，但合入序列以 cherry-pick / merge 顺序为准，*"fix(gateway): probe launchd domain instead of hardcoding user/<uid> (#40831)"*）。**99 个 commit**（85 non-merge + 6 merge + 8 short subject），版本仍 **v0.16.0** 维护 — `pyproject.toml:10 version = "0.16.0"` + `hermes_cli/__init__.py:17-18 __version__ = "0.16.0"` + `__release_date__ = "2026.6.5"` 全部未变（仅 v0.16.0 维护，未发新版）。
>
> **统计**：287 文件变更 / **+19234 / −3449**；类型分布 **59 fix / 11 feat / 6 chore / 3 style / 3 change / 2 test / 1 refactor / 1 perf / 1 docs / 6 merge / 4 短主题（dashboard file panel / 文件夹 dialog / cron 修复 / "Suppress Credit access paused" 等）**。作者 top：Teknium 18 / Brooklyn Nicholson 14 + brooklyn! 9 = 23 / Ben Barclay 6 / xxxigm 5 / teknium1 5 / Shannon Sands 4 / kshitijk4poor 3 + kshitij 3 = 6 / ethernet 3 / Gille 3 / Barron Roth 2 / Siddharth Balyan 2 / mnajafian-nv 2。
>
> **验证基线**：`/tmp/hermes-agent` blobless clone + `git checkout -f a8f404b` 全 checkout @ HEAD，所有 commit hash / file:line / 函数 / 路径经 `git show <sha>` / `Grep` / `Read` 直接实证；4 个并行 research subagent 分主题验证（write-approval / Desktop UI / Dashboard profile builder / Gateway+Agent+CLI）。

## 主题集群（11 大主题，按影响力降序）

1. **Memory + Skills `write_approval` 审批 gate v1 → v2 三连**（feat #38199 → refactor #43354 三态 → 布尔 → fix #43452 三 bug 修复，共 +1814 LOC）
2. **Dashboard 全功能 Profile Builder + 文件浏览器/上传/文件夹对话框**（#39084 五步流 + 5 个 `/api/files/*` 端点 + 拖放上传 + 文件夹对话框）
3. **Desktop UI 革命**（Mac-style 会话切换器 ^Tab/^⇧Tab/^1-9 #43111 + 多窗口 ⌘-click #43219 + VS Code Marketplace 主题 + 可拖动 VS Code 主题终端面板 #42521 + ANSI 16 色 + Cmd-K HUD 重塑 + 侧边栏双模式 #43147）
4. **Desktop 远程附件上传管线 8 件套**（#43109 PR 8 commit：远程会话工作区落地 + 乐观缩略图 + race 关闭 + staging spinner + 16MB toast + sqlite_master 恢复 #43149）
5. **Anthropic thinking-signature recovery 两连 + Error-Classifier 两连**（`9f95f72` 修 api_messages 别名 + `86e10dd` 路由 *"cannot be modified"* 400 + `2ce3ae3` 修 `unsupported parameter` 400 误判为 context_overflow + `19c07c4` GPT-5 系 `max_completion_tokens`）
6. **Gateway 恢复四件**（`a8f404b #40831` launchd 域名探测 gui/<uid>→user/<uid>→managername 缓存 + `5cf6e28 #42675/#43236` 容器重启自启 planned-stop marker + `702f4df #41976` cron ownership 修复 + `984e69f` Telegram QR onboarding 自动重启 + `fa32af8` 并发重启去重）
7. **CLI 跨平台 stdout/stderr UTF-8 + One-shot lifecycle 两连**（`2f19512 #43439` 替 Windows-only `_PYTHONIOENCODING` 路径 + `d03cdd6 #43036` 清理顺序倒置改成 lease 释放前清理 + `f8fd309 #43320` 中断时去重 finalize）
8. **Discord role-authorization 端到端打通 + voice 三件套**（`e5580f4` 修 `DISCORD_ALLOWED_ROLES` 在 gateway 层失效 + `3119008` 不在 voice-off 时自动断开 + `aea0b73` 测试覆盖 + `4eadef1` MagicMock guard + `cd9a9cd #43444` Slack 线程审批 UX）
9. **Skills 体验三件套**（`298bb93 #43398` browse 实时每源进度 ticker + `eee1da4 #43395` ClawHub cold-cache walk 改 bounded + `105625d` 履行 overall_timeout + `9caa12f` frontmatter `name:` 反查 + `fdc9034 #43221` 红队 skill (godmode/obliteratus) 迁出 bundled）
10. **Docker 两连**（`a72bb03 #38749` .dockerignore 排除 ~69MB + 前端构建层独立缓存 + `5d8c44a #30399/#42413` matrix [encryption]/python-olm 预装 + 配 cmake/g++/make）
11. **杂项**（OpenRouter reasoning_effort → verbosity #43436 / TTS Gemini persona + audio tag rewrite / Langfuse base64 data URI redact / MiniMax M3 1M hardcoded / xAI Grok Build 粘贴码 + 8x8 screenshot guard / Curator 共享 atomic writer / Desktop 隐藏 Windows console / `.env` mode 保留 / 半建 venv 自愈 / Dashboard OAuth gate #42578 / TUI titlebar 会话名 #43188 / AGENTS.md Design Philosophy / npm audit 工具链）

---

## 1. Memory + Skills `write_approval` 审批 gate 三连（+1814 LOC）

> 验证：`tools/write_approval.py` / `tools/memory_tool.py` / `tools/skill_manager_tool.py` / `hermes_cli/write_approval_commands.py` / `hermes_cli/cli_commands_mixin.py` / `hermes_cli/commands.py` / `hermes_cli/config.py` / `gateway/slash_commands.py` / `gateway/run.py` @ HEAD `a8f404b` 全实证。

### 1.1 `96af61b feat(memory,skills): approve/deny gate for memory + skill writes (#38199)`（Teknium，+1270 / -11）

第一版三态 gate：`memory.write_mode / skills.write_mode = on | off | approve`：

- **`on`**（default）：write freely，行为不变
- **`off`**：never write；tool 返回 clean disabled result
- **`approve`**：don't commit：
  - **Memory foreground writes**：inline prompt（聊天 bubble 中可 review 的小写入）
  - **Background memory writes**（self-improvement review fork — *"the source of the unprompted 'wrong assumption' saves users reported"*） **+ ALL skill writes**：stage 到 pending store（SKILL.md 太大无法 inline、daemon thread 也不能 block on prompt）

CLI / 任何 messaging 平台都能 review：`/memory pending|approve|reject|mode` + `/skills pending|approve|reject|diff|mode`。Skill review 尊重 size asymmetry — inline 看 one-line gist，full unified diff 出 band（`/skills diff`、dashboard、staged JSON file）。

新文件：`tools/write_approval.py`（gate + pending store）、`hermes_cli/write_approval_commands.py`（shared CLI+gateway handlers，197 行）；gate wire 在单一入口 `memory_tool()` 与 `skill_manage()`，借现有 write-origin `ContextVar` 区分 foreground vs `background_review`。

### 1.2 `095f526 refactor(memory,skills): replace tri-state write_mode with boolean write_approval (default off) (#43354)`（Teknium，+296 / -187）

三态混淆 *"writes enabled"* 与 *"writes gated"* 两个概念：*"`on` (writes flow freely, gate inactive) read like 'gating is on'"*。替为单 boolean，default off：

- `memory.write_approval / skills.write_approval`：
  - `false`（default）— write freely；the approval gate is off（pre-gate behaviour）
  - `true` — require approval：memory foreground prompts inline，memory background-review + all skill writes stage for review

旧 `off = block all writes` 模式被丢弃 — `memory_enabled: false` 已能完全禁用 memory，第三种 *"block"* state 冗余。

变更摘要：
- `tools/write_approval.py`：`get_write_mode/MODE_*` → `write_approval_enabled() -> bool`；`evaluate_gate()` 失去 config-driven `blocked` path（blocked 现在只来自交互式用户拒绝）。当前 HEAD 签名：
  - `write_approval_enabled(subsystem)` @ `tools/write_approval.py:74-89`
  - `evaluate_gate(subsystem, *, inline_summary="", inline_detail="")` @ `:253-312` 返回 `GateDecision`（`allow=True` / `blocked=True` / `stage=True` 三互斥）
  - `current_origin()` @ `:207-223` 委托 `tools.skill_provenance.get_current_write_origin()`，返 `"foreground"`（default）/ `"background_review"`
- `tools/memory_tool.py`、`tools/skill_manager_tool.py`：注释 + 行为跟随
- `hermes_cli/config.py`：`memory/skills write_mode → write_approval (False)`；`_config_version 28→29`，迁移函数 @ `:4760-4786`：`if current_ver < 29:` 迭代 `("memory", "skills")` subsystems → `sub.pop("write_mode")` → `sub["write_approval"] = (old_norm == "approve")`（`approve→True`、`on/off/unset/bool→False`）
- Slash commands：`/memory|/skills mode <on|off|approve>` → `approval <on|off>`（`mode` 保留作 back-compat alias）；`set_mode_fn` callback 现取 `bool`
- `hermes_cli/write_approval_commands.py` / `cli_commands_mixin.py` / `gateway/slash_commands.py` / `commands.py`：handlers + registry args / subcommands 全更新

### 1.3 `70d5d7e fix(memory,skills): repair write-approval inline prompt, gateway staging, and gateway /skills review (#43452)`（Teknium，+248 / -58）

三个 bug：

1. **Inline CLI memory approval never worked** — `tools/write_approval.py:337-381`（`_prompt_inline_memory_approval()`）。原 callback 通过 `prompt_dangerous_approval` 包装、然后 hit `input()` fallback（prompt_toolkit 下 deadlock）→ 静默 deny。修：直接 `choice = callback(command, description, allow_permanent=False)`（`:365-368`），异常包 try/except 返 `None` 转 stage（`:369-373`）
2. **Gateway staging missing for `/skills`** — `gateway/slash_commands.py:2061-2117` 新增 `_handle_skills_command()` 整段（memory 在 `:2019-2059` 已存在）；gateway routing entry @ `gateway/run.py:7068-7069` 新增 `if canonical == "skills": return await self._handle_skills_command(event)`；gateway 上 skills diff 截 3000 char + 指向 CLI/file
3. **Pre-gate validation** — `tools/memory_tool.py:684-692` 把参数校验（content / old_text 等）从 gate 之后挪到 gate **之前**，避免 broken writes stage 后才 fail at approve time

Slash 命令在 HEAD（`hermes_cli/commands.py:173-176`）：

```
CommandDef("memory", ..., subcommands=("pending", "approve", "reject", "approval"))
CommandDef("skills", ..., subcommands=("search", "browse", "inspect", "install", "audit",
                                       "pending", "approve", "reject", "diff", "approval"),
           gateway_config_gate="skills.write_approval")
```

`approval`（alias `mode` for back-compat）、`approve`（alias `apply`）、`reject`（alias `deny`/`drop`）在 `hermes_cli/write_approval_commands.py:84-97` 显式 normalize。

配置默认：`memory.write_approval: False` @ `hermes_cli/config.py:1689`、`skills.write_approval: False` @ `:1805`、`_config_version: 29` @ `:2489`。

---

## 2. Dashboard 全功能 Profile Builder + 文件浏览器 / 上传 / 文件夹对话框

> 验证：`hermes_cli/web_server.py` / `web/src/pages/ProfileBuilderPage.tsx` / `web/src/pages/FilesPage.tsx` / `docs/design/profile-builder.md` / `web/src/lib/api.ts` / `hermes_cli/dashboard_auth/prefix.py` @ HEAD 全实证。

### 2.1 `d986bb0 feat(dashboard): full-featured profile builder (model + skills + MCPs) (#39084)`（Teknium）

**后端（POST `/api/profiles` 扩展）**：`hermes_cli/web_server.py:8001-8105` `create_profile_endpoint` 接收三个新 optional fields：

- `mcp_servers: List[MCPServerCreate]` → 经 `_write_profile_mcp_servers()` @ `:7903-7952` 写入 profile 的 `config.yaml`，用 `set_hermes_home_override(str(profile_dir))`（`:7918`）profile-scope（镜像现有 `_write_profile_model` 模式）
- `keep_skills: List[str]` → **replace semantics**：经 `_disable_unselected_skills()` @ `:7955-7988` 扫描 profile `skills/` 目录、加载已装 skills 名、disable 不在 `keep` list 的（`save_disabled_skills()` @ `:7985`）；同样 `HERMES_HOME` override（`:7971`）
- `hub_skills: List[str]` → async 经 `_spawn_hermes_action()` @ `:8079-8095` spawn `hermes -p <name> skills install <id>`（`:8084-8085`），subprocess `-p` flag scope，因 `skills_hub.SKILLS_DIR` import-time-bound 不能用 runtime override 重定向；返 `{identifier, pid}` list 供 async polling

全部 best-effort 在 profile dir 创建后 apply — 任何一步 hiccup 都不 500 create。设计 doc：`docs/design/profile-builder.md`（144 行）。

**前端**：`web/src/pages/ProfileBuilderPage.tsx`（611 行）五步流：

1. **Identity** `:55-57` — name + description
2. **Model** `:59-63` — provider+model picker，调 `api.getModelOptions()` `:97`
3. **Skills** `:65-76` — built-in/optional toggle（replace semantics，default = full bundle） + hub search/add，调 `api.getSkills()` `:117` + `api.searchSkillsHub()` `:139`
4. **MCPs** `:78-85` — 内联 HTTP/stdio servers
5. **Review** — blueprint 单 POST `api.createProfile()` `:221`

Route：`web/src/App.tsx` 注 `/profiles/new`（页面级，不在 nav）；create 后 toast 显示 `hub_installs` 数量。

### 2.2 文件浏览器 backend 5 端点（`6fe4821 Add dashboard file browser paths`、`dd60c49 Add dashboard file drop upload panel`、`3acf731 Move folder creation into dialog`）

- `GET /api/files` @ `hermes_cli/web_server.py:1063-1088` (`list_managed_files`)：optional `path` query，返 `ManagedFilesResponse`（dirs first + files、parent link、policy metadata）
- `GET /api/files/read` @ `:1091-1121`：read as base64 data URL，max **100MB**（`_MANAGED_FILE_MAX_BYTES` `:1103`）
- `POST /api/files/upload` @ `:1124-1146`：`ManagedFileUpload(path, data_url, overwrite)`，`target.write_bytes()` `:1135`
- `POST /api/files/mkdir` @ `:1149-1167`：`ManagedDirectoryCreate(path)`，`parents=True` `:1156`
- `DELETE /api/files` @ `:1170-1192`：`ManagedFileDelete(path, recursive)`，`shutil.rmtree()` 或 `target.rmdir()`

**Managed files policy** @ `:901-971` `_managed_files_policy()`：托管部署锁 `/opt/data` 或读 `HERMES_DASHBOARD_FILES_ROOT` env override；本地 dashboard 限 home 目录。

**前端**：`web/src/pages/FilesPage.tsx`（406 行）：
- 路径导航（本地 input bar、托管 badge）
- 列表（类型图标、size、modified date）
- **文件拖放上传面板** `:71-80`（`dd60c49`）：`<input type="file" multiple>` + drag handlers + `uploadFiles()` `:108-125`
- **文件夹对话框** `:58 + :155-168`（`3acf731`）：`folderName` state + `createDirectory` handler 改用 Input+Button 替原 inline modal
- Download / delete with recursive
- API 全在 `web/src/lib/api.ts:328-353`：`listFiles / readFile / uploadFile / createDirectory / deleteFile`

Route：`web/src/App.tsx` 注 `/files`。

### 2.3 `63a421d fix(dashboard): _require_token endpoints all 401 behind the OAuth gate (#42578)`（Teknium）

`_require_token` decorator @ `hermes_cli/web_server.py:250-277`：
- 若 `request.app.state.auth_required == True`（OAuth/gated 模式）：check `request.state.session`（`gated_auth_middleware` 注入）；有则 allow `:274`、无则 401 `:275`
- 否则（loopback / `--insecure`）：legacy `_SESSION_TOKEN` header 经 `_has_valid_session_token()` `:276`

经它 cover 的端点（14 个）：`/api/providers/validate`、`/api/env/reveal`、`/api/providers/oauth/{provider_id}` 全套（delete / start / submit / session delete）、`/api/dashboard/plugins/hub`、`/api/dashboard/agent-plugins/{name}/*`（install / enable / disable / update / delete）、`/api/dashboard/plugin-providers`（PUT / visibility）。

Tests `tests/hermes_cli/test_dashboard_auth_middleware.py:194-279`：登录后 invalid plugin id 应得 **400**（handler 触发，不是 401）；无 cookie 仍 401。

### 2.4 `ea7981e fix(dashboard): point webhook-disabled hint at Channels page (#43324)`

`web/src/pages/WebhooksPage.tsx:379-389` — 原文 *"The webhook platform must be enabled in your messaging settings before..."* → *"The webhook platform must be enabled on the Channels page before you can create subscriptions. Enable it there, then return to this page."*（消除不存在的 *"messaging settings"* 引用）。

### 2.5 `7df3aa3 fix(dashboard-auth): warn when public_url override is silently rejected (#43214)`

`hermes_cli/dashboard_auth/prefix.py:31-201`：
- Dedup cache `_warned_malformed_public_urls: set` `:39` 按 `(source, value)` 去重
- `_warn_if_malformed()` @ `:42-72`：若 raw value normalize 为空（缺 scheme）→ WARNING 命名 source（`HERMES_DASHBOARD_PUBLIC_URL` env / `dashboard.public_url in config.yaml`） + 无效值 + 正确 scheme 范例
- `resolve_public_url()` @ `:192-201`：先 env、再 config.yaml，每步 malformed 时 warn 并 fall through

Tests @ `tests/hermes_cli/test_dashboard_auth_prefix.py:389-472` 覆盖 dedup（5 次同值 = 1 warning）+ 有效 URL 无 warning。

---

## 3. Desktop UI 革命（10+ 件，全 file:line 实证 @ HEAD `a8f404b`）

### 3.1 `ab5f1a1 feat(desktop): Mac-style session switcher (^Tab / ^⇧Tab / ^1-9) (#43111)`（brooklyn!）

绑 `session.next / session.prev` 到 `Control+Tab / Control+Shift+Tab`，用独立的 `ctrl` modifier token（macOS 上是字面 Control，不是 Cmd — OS 保留）。加 `^1…^9` positional jumps 镜像 profile `⌘1…⌘9`。

Mac-style 交互：
- 快速 `^Tab` 点击：keydown 时跳，无 HUD（即使 Ctrl 仍按）
- Hold Tab ~220ms 或在 Ctrl 仍按时再点 Tab → compact HUD
- `Ctrl↑` 确认高亮、`Esc` 取消、行可点击（`^+click` 安全）
- Recency-ordered list 在打开时快照、按 stored session id 循环

**文件**（11 个，+570 / -26）：
- `apps/desktop/src/app/hooks/use-keybinds.ts:135-137` 绑定 `session.next/.prev` handlers；`:89-97` 定义 `session.slot.1` 到 `session.slot.9` → `slotSessionId(slot)`
- `apps/desktop/src/lib/keybinds/combo.ts:94-96` 检测物理 Control on macOS、emit `ctrl` token（与 `mod` 区分）；`:114-116` 非 macOS 上 normalize `ctrl → mod`
- `apps/desktop/src/store/session-switcher.ts:10` 定义 `SWITCHER_REVEAL_MS = 220`；`:77-82` 在第一次 Tab 时快照、按 stored session id 循环；`:99` 从快照取 `highlightedSessionId()`；`:46-56` Tab hold 状态；`:30-43` 220ms timer schedule reveal；`:60-96` open-or-advance logic + pending-browse state
- `apps/desktop/src/app/session-switcher.tsx` 97 行新 HUD 组件 + `combo.test.ts 86 行` + `session-switcher.test.ts 115 行`

### 3.2 `b96bd48 feat(desktop): open any chat in its own window (#43219)`（brooklyn! + tim404x）

把 session 弹出到独立 focused 窗口做 side-by-side 工作。Secondary window 在 HashRouter `#` 之前加 `?win=secondary` query flag 加载 session route；drop 全局 sidebar + install/onboarding overlays、只渲染单 chat、共享一个 local gateway over WS（无 backend 重复）。Main process 按 sessionId key window — 重开就 focus 已有的、关闭自清。

打开方式：
- **⌘-click (mac) / ⌃-click (win/linux)** sidebar session — 通用 *"open in new window"* gesture；archive 从 modifier-click 挪到 `⋯` / right-click 菜单（不再 misfire）
- session `⋯` 与 context 菜单的 *"New window"*（link-external icon，跨 en/ja/zh/zh-hant i18n）

独立 window 无 left rail，AppShell 把它的 edge 当 uncovered + 应用 titlebar inset — chat title 让出 macOS 红绿灯。

**文件**（+~470）：
- `apps/desktop/electron/session-windows.cjs:26-84` `createSessionWindowRegistry()` 按 sessionId key window、防重（`:43-56`）、关闭自清（`:66-71`）；`:14-24` 把 `?win=secondary` build 在 `#route` 之前（让 HashRouter 在 `window.location.search` 看到）
- `apps/desktop/src/store/windows.ts:12-28` 从 `URLSearchParams(window.location.search)` 读 flag
- `apps/desktop/src/app/shell/app-shell.tsx:7` titlebar inset 当无 sidebar 时
- `apps/desktop/src/app/chat/sidebar/session-actions-menu.tsx:14` Archive 移到 menu；`session-row.tsx:9` 用 modifier-click 开窗
- `apps/desktop/electron/main.cjs +125` + `preload.cjs +1` + tests `session-windows.test.cjs 165 行`

### 3.3 `27a3211 feat(desktop): install any VS Code theme from the Marketplace` + `8f73d0d feat(desktop): resizable VS Code-themed terminal pane + palette polish (#42521)` + `833410e feat(desktop): theme the terminal ANSI palette + restyle the Cmd-K / Ctrl-Tab HUDs`

**VS Code Marketplace 主题**：
- `apps/desktop/src/themes/install.ts:1-8` Marketplace fetcher 在 Electron main process（`electron/vscode-marketplace.cjs`）做 network + unzip
- `:20-28` 通过 `parseVscodeTheme()` 解 JSONC（`/tmp/hermes-agent/apps/desktop/src/themes/vscode.ts`） → `convertVscodeColorTheme()` 转换
- `:27` 调 `installUserTheme(theme)` (from `user-themes.ts`) 持久化
- `:41-73` Marketplace builder 把多 color themes fold 成单 family（light + dark variants）：`light.palette → colors`、`dark.palette → darkColors`、每变体 terminal palette（`:58-62`）

**ANSI 16 色 palette 提取**：`apps/desktop/src/themes/vscode.ts:106-181` 从 `terminal.ansi*` tokens 提取 16-color ANSI slots；base-8 上 all-or-nothing（`:160`）；`:51` 映射 terminal palette 到 theme.terminal on install；`:105` 注释明确终端 pane 故意不带背景色（保留 live surface 透明度）

### 3.4 `7803cbf style(desktop): use the nous overlay surface (--stroke-nous + --shadow-nous) for the HUDs` + `45e1689 fix(desktop): apply the shared HUD tokens to the marketplace submenu`

**Cmd-K / Ctrl-Tab HUDs 共享 surface 样式**：`apps/desktop/src/app/floating-hud.ts:5-22`：
- `:5` `HUD_POSITION = 'fixed left-1/2 top-3'`
- `:9` `HUD_SURFACE` 用 `--stroke-nous` border + `--shadow-nous` shadow（无 dim/blur backdrop）
- `:21` Section headers 用 `--theme-primary`（品牌色）
- `:22` Uppercase / tracked / sidebar-label 风格

### 3.5 `8bb6529 fix(desktop): sidebar sections never overlap — two-mode CSS scroll + collapse/cap groups (#43147)`

**侧边栏 tall vs compact 双模式**：`apps/desktop/src/app/chat/sidebar/index.tsx:140-148`：
- `:140` `@media (max-height: 768px)` 触发 `compact` variant
- `:145` `COMPACT_FLAT = 'compact:max-h-none compact:overflow-visible'` flatten 所有 section
- `:141-144` Tall 模式 = 每 section cap 独立 scroller；compact = 共享 stack scroll
- `:148` 非 session group 应用 `GROUP_BODY = 'overflow-y-auto overscroll-contain ' + COMPACT_FLAT` — 默认折叠、展开 id 按 platform 持久

### 3.6 `c1308eb style(desktop): filled SVG glyphs for in-thread tool icons` + `e807546 style(desktop): render in-thread tool codicons as filled glyphs` + `f222bd2 Merge PR #43430`

In-thread tool icons 从 outline 换填充 codicons。

### 3.7 `3827367 fix(desktop): carve sticky user bubbles out of the titlebar drag region`

Sticky user bubble 与 titlebar drag region 分开（避免点 bubble 时拖窗）。

### 3.8 `d339653 feat(tui): include session name in the terminal titlebar (#43188)`

**Titlebar 组装**：`ui-tui/src/domain/paths.ts:27-40`：
- `composeTabTitle(marker, sessionName, model, cwd)` 格式 `<marker> <session name> · <model> · <cwd>`
- 省略 empty name/cwd；name 在 28 字符截（`:35`）
- `:39` marker 始终用纯空格粘第一段

**集成到 live titlebar**：`ui-tui/src/app/useMainApp.ts:567` 调 `composeTabTitle()` 用 `ui.sessionTitle`（来自 `session.active_list` 轮询）；`:532-542` 仅在 `sessionTitle` 改变时更新 `$uiState`（idle-flicker guard）

### 3.9 小修

- **`258d240 fix(desktop): scope thinking disclosure pending state (#43197)`**：`apps/desktop/src/components/assistant-ui/thread.tsx:476-483` 把 `pending` flag 限到正在运行的 message + running parts（`:479` `s.message.status?.type === 'running'`），避免之前完成的 reasoning 再展开
- **`59ea2f9 fix(desktop): always show the Manage-profiles overflow (#42871)`**：`apps/desktop/src/app/chat/sidebar/profile-switcher.tsx:272-276` 无条件 render *"Manage"* pill（不再 gate 在 `multiProfile` 后），让单 profile 用户也能编辑默认 SOUL.md
- **`1770263 fix(desktop): honor default project directory for new sessions (#43234)`**：`apps/desktop/src/store/session.ts:14-76` 从 Settings 同步配置的默认 cwd；`ensureDefaultWorkspaceCwd()` 在 boot 时无 active session 则 seed `$currentCwd`；新 session 经 `workspaceCwdForNewSession()` 读
- **`8b84d82 fix(desktop): send on Enter from live editor text, not stale composer state (#39639)`**：`apps/desktop/src/app/chat/composer/index.tsx:823` 在 Enter handler 里 `draftRef` 从 editor DOM 重同步（`composerPlainText(editorRef.current)`）再决定 submit vs queue，关掉让快速键入消息悄无声息丢的 render-lag 缝
- **`6de3963 fix(desktop): keep model runtime state per session (#43702)`**：`apps/desktop/src/app/session/hooks/use-session-actions.ts:220-279` 把 model / provider / reasoningEffort / serviceTier 等存进 `sessionState` 同时还设全局 atoms；`:232, 237, 256, 261` 每 session 持久；`:464-490` resume session 时通过 `syncSessionStateToView()` 恢复 footer state

## 4. Desktop 远程附件上传管线（#43109 八件套 + #43149）

> Source verification 来自专门 subagent + 直接 git show 验证（@ HEAD `a8f404b`）。

### 4.1 `aecdacb Merge PR #43109 from NousResearch/fix/desktop-remote-attach-drops` — 整条 *"drops uploaded to remote sessions"* PR 链

PR `#43109` 包含 8 个 commit（最早到合入顺序，全 brooklyn!）：

1. **`4906dcf fix(desktop): stage dropped files into the remote session workspace`** — drop 文件到 remote 会话时 stage 到工作区
2. **`153060e fix(desktop): render optimistic image thumbnails from in-hand base64`** — 用 in-hand base64 渲染 optimistic 缩略图（不等服务器回 url）
3. **`72154ad perf(ci): cache uv + use uv sync in tests workflow`** — CI 缓存 uv + tests workflow 用 `uv sync`
4. **`891c9a6 fix(desktop): close eager-upload races flagged in review`** — eager-upload race 关闭
5. **`b021497 fix(desktop): show a staging spinner in the edit composer while OS drops upload`** — edit composer 在 OS drops 上传时显示 staging spinner
6. **`29147af fix(desktop): friendlier toast when a remote attachment exceeds the 16MB cap`** — 远程附件超 16MB 时显 friendlier toast
7. **`218452b fix(state.db): recover from malformed sqlite_master so hidden sessions reappear (#43149)`** — `hermes_state.py:229-368` 新 `repair_state_db_schema()` 二段恢复（dedup sqlite_master keep lowest rowid、或 drop all `messages_fts*` schema objects + VACUUM） + `is_malformed_db_error()` discriminator；`:633-688` `SessionDB.__init__()` 自动 heal malformed-schema 打开（process-guarded repair-once + reopen）；`hermes_cli/doctor.py` 与 `hermes_cli/main.py` 把 repair 整合进 `hermes doctor --fix` 与 `hermes sessions repair` 命令
8. **`7ffc216 fix(agent): make a binary @file: reference actionable instead of a dead end`** — 见 §11.12

## 5. Anthropic thinking-signature 恢复两连 + Error-Classifier 两连

### 5.1 `9f95f72 fix(agent): strip api_messages in thinking-signature recovery so the retry actually omits thinking blocks`（0xyg3n）

旧 recovery 路径在 `agent/conversation_loop.py:2224-2262` pop `reasoning_details` 后继续 retry，但有两个缺陷：
1. **strip 没到 wire payload**：`api_messages` 在 turn 起始时一次性 shallow-copy（约 line 919）；每个 `api_messages` 条目仍持有相同 `reasoning_details` list 的引用；`build_api_kwargs` 每个 retry 迭代消费的是 `api_messages` 而非 `messages`，所以即使 pop 了 `messages` 上的 `reasoning_details`、wire payload 仍带 thinking 块
2. **canonical store 也被污染**：旧 strip 修改 `messages`（canonical store），错误写到 state.db

修：对 `api_messages` 做 pop（API-call-time list，每次 retry 重建），canonical `messages` 不动。

### 5.2 `86e10dd fix(agent): route 'thinking blocks cannot be modified' 400 to recovery`（Ian Culling）

Anthropic 返 400：*"thinking or redacted_thinking blocks in the latest assistant message cannot be modified. These blocks must remain as they were in the original response."* — Classifier `agent/error_classifier.py:553-576` 的 `thinking_signature` 分支只匹配 `"signature"` substring，所以这个 variant fall through 到非可重试 client error 硬中止 turn — 即使 strip-reasoning_details-and-retry 能 heal。修：扩展匹配 *"cannot be modified"* + *"must remain as they were"*（gated on `"thinking"`），让它走 strip-and-retry。

### 5.3 `2ce3ae3 fix(error-classifier): don't misclassify unsupported-param 400s as context overflow`（Teknium）

GPT-5 拒 `max_tokens` 时返 400，消息含 literal `"max_tokens"` substring — 是 `_CONTEXT_OVERFLOW_PATTERNS` 之一。`_classify_400` 的 400 path 检查 overflow patterns **先于** any request-validation check（后者只存在于 5xx path）→ parameter error 被 routed 进 compression loop、用相同 bad param 重发、最终 *"Cannot compress further"* on tiny context。

修：`agent/error_classifier.py:969-996` 把 request-validation guard（unsupported / unknown parameter）**抬到** context-overflow check 之上：
```python
if (
    any(p in error_msg for p in _REQUEST_VALIDATION_PATTERNS
        if p != "invalid_request_error")
    or error_code_lower in {"unknown_parameter", "unsupported_parameter"}
):
    return result_fn(FailoverReason.format_error, retryable=False, should_fallback=True)
```
显式排除通用 `invalid_request_error` code（OpenAI 也在真 overflow 400 上盖这个 code），保留真 overflow 仍走 compression。配 `19c07c4` 在源头修好 bad request。

### 5.4 `19c07c4 fix(params): send max_completion_tokens for newer OpenAI families on custom endpoints`（Xiangji aka Llugaes）

`utils.py` 加 `model_forces_max_completion_tokens(model)` helper — prefix-match 归一化 model name 上的 `gpt-4o` / `gpt-4.1` / `gpt-5+` / `o1`-`o4` 系列。`run_agent.py:_max_tokens_param()` 与 `agent/auxiliary_client.py:auxiliary_max_tokens_param()` 现在 OR 入这个 model-name check — 让第三方 OpenAI 兼容端点上跑新族系也能得到正确 `max_completion_tokens` param。

---

## 6. Gateway 恢复四件

### 6.1 `a8f404b fix(gateway): probe launchd domain instead of hardcoding user/<uid> (#40831)`（Tranquil-Flow）

`hermes_cli/gateway.py:3074-3147`，`_launchd_domain()` 三步探测：

1. 先 try `gui/<uid>`（Aqua sessions） — `launchctl print {gui_domain}/{label}` 5s timeout 成功即返
2. 失败则 try `user/<uid>`（Background/SSH sessions） — 同样 5s timeout
3. 都无该 service 时 fallback `launchctl managername` 作 heuristic
4. 结果缓存到 `_resolved_launchd_domain: str | None = None` module global（process lifetime；每次 `hermes gateway start/stop/status` invocation 独立）

之前为 #23387 改成硬编码 `user/<uid>` 支持 macOS 26+ Background/SSH，但破坏 Aqua sessions（`gui/<uid>` 是唯一 working 域、`user/<uid>` 不能 bootstrap 或管理 service）。回归测试 `tests/hermes_cli/test_gateway_service.py +129 行` 覆盖全四 path + 缓存行为。

### 6.2 `5cf6e28 fix(gateway): auto-start after container restart via planned-stop marker (#42675) (#43236)`

`gateway/run.py:1962-1995` `GatewayRunner` 加 `_signal_initiated_shutdown` field；`:5962-6022` `_stop_impl()` 在 shutdown 是 signal-initiated（unmarked SIGTERM）而非 operator-requested 时持久 `gateway_state=running` 而非 `stopped`，让容器重启自启 gateway。`hermes_cli/service_manager.py:742-789` `S6ServiceManager.stop()` 现在提取 supervised PID + 写 planned-stop marker、再 `s6-svc -d`，让 gateway 识别 stop 为故意的。

### 6.3 `702f4df Repair cron ownership on container restart (#41976)`

`docker/stage2-hook.sh:265-270` 在原有 profiles ownership reset 旁加 `chown -R hermes:hermes "$HERMES_HOME/cron"` — cron scheduler state 文件在 root-context 写后仍对非特权 runtime 可读。

### 6.4 `984e69f Auto-restart gateway after Telegram QR onboarding` + `fa32af8 fix: dedupe concurrent gateway restarts + surface restart outcome in onboarding UI`

`984e69f`：`hermes_cli/web_server.py:3751-3773` 新 `_restart_gateway_after_telegram_onboarding()` — onboarding config save 完成后 spawn gateway restart child + 返 restart 状态供 UI 报告。+ `web/src/lib/api.ts`（+6 行） + `web/src/pages/ChannelsPage.tsx`（+23 行）。

`fa32af8`：
- `hermes_cli/web_server.py:1375-1389` 新 `_spawn_gateway_restart()` — 已有 live in-flight `hermes gateway restart` child 时复用、不 spawn 第二个 racing 的
- `web/src/pages/ChannelsPage.tsx` 轮询 `/api/actions/gateway-restart/status` 显示 non-zero exit code via manual-restart banner

### 6.5 其他 gateway / 平台修

- **`6a30cfc fix(gateway): stop typing before post-delivery callbacks (#37556)`** — `gateway/platforms/base.py:4462-4500` 把 typing-stop 调用前移到 post-delivery callbacks 之前，并用 `asyncio.wait_for` 30s timeout — 让 hung callback 不会 indefinitely 留下 typing refresh task
- **`9ca9697 fix(gateway): return tuple from voice transcription on placeholder caption (#42090)`** — `gateway/run.py:11541` 之前 Discord 空内容 placeholder 在 transcription 后被 strip 时返 bare `prefix` 字符串而非 `(prefix, successful_transcripts)` tuple → 调用方 `text, transcripts = await ...` 解包失败。修正返 tuple
- **`218452b fix(state.db): recover from malformed sqlite_master so hidden sessions reappear (#43149)`** — 见 §4.1 第 7 件
- **`cd9a9cd fix(gateway): Slack approval UX in threads — block-size overflow + typed-prefix instruction text (#43444)`** — `gateway/platforms/base.py:12` 加 `typed_command_prefix` capability field（default `"/"`）；`gateway/platforms/slack.py:send_exec_approval` 与 approval 方法把 command preview budget 限到 3000-char block cap；`gateway/slash_commands.py` 文本 fallback 用 adapter `typed_command_prefix` + 接受 `!` 前缀回复
- **`b4170f3 fix(cron): don't strict-scan script-injected output in no-skills jobs (#43223)`** — `cron/scheduler.py:1118-1346` `_build_job_prompt()` 加 `has_injected_data` flag 传给 `_scan_assembled_cron_prompt()`，在 script stdout 或 upstream-job output 注入时选 looser pattern tier（同 skill-content tier），用户 prompt 本身仍严格扫做 defense-in-depth
- **`2903615 fix(terminal): lazy-parse docker env config (#42733)`** — `tools/terminal_tool.py:1067-1095` 把 Docker-only JSON env var 解析包在 conditional 里 — local/ssh backends 忽略 malformed `TERMINAL_DOCKER_ENV` 等，只在 Docker backend 显式选时才 validate；防止 stale config 破坏所有 terminal 操作
- **`93340fa fix(tui_gateway): honor target profile's terminal.cwd on desktop profile switch (#40892)`** — `tui_gateway/server.py:688-722` 新 `_profile_configured_cwd()` helper 读 profile 自己的 `terminal.cwd` from `config.yaml`（拒绝 placeholder/missing path）；`_completion_cwd()` 现在优先选 bound profile 的 configured cwd over stale launch-profile `TERMINAL_CWD`，优先级：explicit client cwd > existing session cwd > profile cwd > env var > `os.getcwd()`

---

## 7. CLI 跨平台 UTF-8 + One-shot lifecycle 两连

### 7.1 `2f19512 fix(cli): repair non-UTF-8 stdout/stderr on all platforms, not just Windows (#43439)`

`hermes_cli/__init__.py:21-89` 删 `sys.platform != "win32"` gate — UTF-8 repair 跨平台运行：
- 优先 `TextIOWrapper.reconfigure()` in-place 修流（保留 object identity）
- fallback 用 `closefd=False` 重新打开 fd
- 仅在 repair 实际发生时设 `PYTHONUTF8/PYTHONIOENCODING` env hints（健康 UTF-8 host 上 minimal footprint）

### 7.2 `d03cdd6 fix(cli): run one-shot query cleanup before lease release (#43036)`

`cli.py:952-1018` 把 finalize 抽到 `_notify_session_finalize()` helper、新 `_finalize_single_query()` wrapper 顺序：finalize → `_run_cleanup(notify_session_finalize=False)` → release（memory provider shutdown 必须先于 lease 释放）；`cli.py:13619` 调 `_finalize_single_query()` 而非 bare `_release_active_session()`。

### 7.3 `f8fd309 fix(cli): prevent duplicate one-shot finalize on interrupted cleanup (#43320)`

`cli.py:893-895` 加 `_single_query_finalize_attempted_session_ids` set 追踪已 finalize 的 session；`:1020-1025` `_should_emit_cleanup_session_finalize()` guard 让 atexit cleanup 不重 emit；`:1082-1095` `_notify_single_query_session_finalize()` 现在 finally block 中 check + add 到 set。

---

## 8. Discord role-authorization 端到端 + voice 三件套 + Slack 审批 UX

### 8.1 `e5580f4 fix(discord): propagate role_authorized flag so DISCORD_ALLOWED_ROLES works end-to-end`（Joel Chan）

`DISCORD_ALLOWED_ROLES` 被 Discord adapter `_is_allowed_user` 检查、但 `gateway._is_user_authorized` 只读 `DISCORD_ALLOWED_USERS` — role-authorized 用户在 gateway 层 *"Unauthorized user"* 被拒，尽管 adapter gate 已通过。

源码 file:line：
- `gateway/session.py:94` `SessionSource` 加 `role_authorized: bool = False`
- `gateway/platforms/base.py` `build_source()` 接受 `role_authorized` param
- `plugins/platforms/discord/adapter.py:_handle_message()` 计算并传 `_role_authorized` flag
- `gateway/authz_mixin.py:207-211` `_is_user_authorized()` 早在 user-ID allowlist 之前 check `source.role_authorized is True`

### 8.2 `3119008 fix(discord): don't auto-disconnect voice when reply mode is off`（+ `aea0b73 test(discord): cover voice timeout under voice-off mode`）

`gateway/run.py:9367-9374` wire `_voice_mode_getter` callback 到 adapter，让它看到 live voice-reply mode；`plugins/platforms/discord/adapter.py:2270-2286` inactivity handler check `_voice_mode_getter()` 在 mode == "off" 时跳过 disconnect；`adapter.py:2414-2421` 用户说话时 reset timer，active listener 不被中途断开。`aea0b73` 在 `tests/gateway/test_voice_command.py` 加测试。

### 8.3 `4eadef1 fix: guard role_authorized check against MagicMock test sources`

`gateway/authz_mixin.py:215` 把 `role_authorized` check 从 `getattr(source, "role_authorized", False)` 改为 `getattr(source, "role_authorized", False) is True` — 让 MagicMock test source 不自动 truthy 通过 authorization gate。

### 8.4 `cd9a9cd fix(gateway): Slack approval UX in threads — block-size overflow + typed-prefix instruction text (#43444)`

见 §6.5 — `gateway/platforms/base.py:12` 加 `typed_command_prefix`（default `"/"`）+ Slack adapter block-size cap + `gateway/slash_commands.py` `!` bang prefix fallback。

---

## 9. Skills 体验五件套

### 9.1 `298bb93 feat(skills): show live per-source progress while browsing (#43398)`（Teknium）

`do_browse` 之前 spin *"Fetching skills..."* 直到全部 source 完，慢 source 看着像 hang。`parallel_search_sources` 已 expose `on_source_done(sid, count)` callback — wire 进 status line tick off live：

```python
# hermes_cli/skills_hub.py:351-374
with c.status("[bold]Fetching skills from registries...") as status:
    _done: List[str] = []
    def _on_source_done(sid: str, count: int) -> None:
        _done.append(f"{sid} ({count})")
        status.update("[bold]Fetching skills from registries...[/]  "
                      f"[dim]done: {', '.join(_done)}[/]")
    all_results, source_counts, timed_out = parallel_search_sources(
        sources, query="", per_source_limits=_PER_SOURCE_LIMIT,
        source_filter=source, overall_timeout=30, on_source_done=_on_source_done,
    )
```

Page 仍在全 set merge + trust-sort 后**一次性** render — browse 的 official-first ordering 与 pagination contract 不变。

### 9.2 `eee1da4 fix(skills): bound ClawHub catalog walk to requested page on cold start (#43395)`（Teknium）

Browse 渲染一页、但 cold-cache fallback 走完整个 50k+ ClawHub catalog 然后 slice 前 N — 12s budget band-aid 后纯浪费。`_load_catalog_index` 加 `max_items`：browse empty-query path bound walk 到 limit early-stop；offline index builder 仍传 `limit=0`（unbounded）走完。Bounded walk 是 partial 所以不写共享 full-catalog cache（同 budget-truncated case 的 poison-guard）。

### 9.3 `105625d fix(skills): honour overall_timeout and bound ClawHub catalog walk`（briandevans）

`parallel_search_sources` 接收 `overall_timeout` 但从不 honor 它。`ThreadPoolExecutor` 在 `with ... as pool` 中 run，`__exit__` 会等所有 future。修：履行 timeout。

### 9.4 `9caa12f fix(skills): resolve skill_view by frontmatter name when dir name differs`（kshitijk4poor）

`skills_list()` 显示 frontmatter `name:`、但 `skill_view()` 只匹配 on-disk dir name（Strategy 2）。当 skill 的 dir name 与 frontmatter name 不同 → view 找不到。修：加 frontmatter `name` 反查。

### 9.5 `fdc9034 chore(skills): move red-team skills (godmode, obliteratus) to optional-skills — Anthropic classifier (#43221)`

红队 skills（godmode、obliteratus）从 bundled catalog 移到 optional-skills（Anthropic classifier）。

---

## 10. Docker 两连

### 10.1 `a72bb03 fix(docker): optimize image size — .dockerignore, drop dev deps, split build layers (#38749)`（Davy）

三处变更（PR 最初有 [docker] extra 后 revert）：

1. **`.dockerignore` 加排除 ~69MB**：
   ```
   apps/                # Desktop Tauri/Electron source
   tests/               # test suite
   website/  docs/      # Docusaurus + docs
   assets/  infographic/
   plugins/hermes-achievements/docs/
   nix/  flake.nix  flake.lock  packaging/
   plans/  .plans/
   acp_registry/
   .env.example  .envrc  .gitattributes  .hadolint.yaml  .mailmap
   LICENSE
   ```

2. **`Dockerfile` 分层缓存**：
   ```dockerfile
   # ---------- Frontend build (cached independently from Python source) ----------
   # Copy only the frontend source trees first so that Python-only changes don't
   # invalidate the (relatively slow) web + ui-tui build layer.
   COPY web/ web/
   COPY ui-tui/ ui-tui/
   RUN cd web && npm run build && \
       cd ../ui-tui && npm run build
   ```
   Python-only commit 不再 invalidate frontend build layer。

3. PR 第一版的 `[docker]` extra（= `[all]` 去掉 `[dev]`）实际是 no-op round-trip 后被 revert，最终保留 `--extra all`。

构建用 apt 包（gcc / python3-dev / libffi-dev / libolm-dev）仍在 final image。真 multi-stage builder→runtime 是更大的 refactor、分开 track。

### 10.2 `5d8c44a fix(docker): pre-install matrix deps in Docker image (#30399) (#42413)`（Evi Nova）

Matrix gateway 需 `mautrix[encryption]` → `python-olm`。`python-olm` 因缺 Windows/macOS wheel 被排出 `[all]`，但 Linux amd64/arm64 有 manylinux 二进制 wheel — Docker 镜像只跑 Linux 所以加 `--extra matrix` 安全：

```diff
-    apt-get install -y --no-install-recommends \
-    ca-certificates curl iputils-ping python3 python-is-python3 ripgrep ffmpeg gcc python3-dev python3-venv libffi-dev libolm-dev procps git openssh-client docker-cli xz-utils && \
+    apt-get install -y --no-install-recommends \
+    ca-certificates curl iputils-ping python3 python-is-python3 ripgrep ffmpeg gcc g++ make cmake python3-dev python3-venv libffi-dev libolm-dev procps git openssh-client docker-cli xz-utils && \
...
-RUN uv sync --frozen --no-install-project --extra all --extra messaging --extra anthropic --extra bedrock --extra azure-identity --extra hindsight
+RUN uv sync --frozen --no-install-project --extra all --extra messaging --extra anthropic --extra bedrock --extra azure-identity --extra hindsight --extra matrix
```

加 `g++ make cmake` 给 `python-olm` native build；`libolm-dev` 已在 runtime linking line。tests `tests/tools/test_dockerfile_pid1_reaping.py:172-203` 加 `test_dockerfile_preinstalls_matrix_dependencies` 与 `test_dockerfile_installs_matrix_native_build_dependencies`。

---

## 11. 杂项（待 agent 输出 / 已验证）

### 11.1 OpenRouter reasoning_effort → verbosity 适配 Anthropic（#43436）

`183d86b fix(openrouter): route reasoning_effort to verbosity for adaptive Anthropic models (#43436)` — `agent/openrouter.py` 与 `run_agent.py` 把 `agent.reasoning_effort` wire 到 top-level `verbosity` field（OpenRouter Anthropic 调用上 `reasoning_effort` 被忽略），让请求的 effort 等级实际影响 adaptive thinking 而非静默回 high 默认。

### 11.2 TTS Gemini 两连

- `5718811 feat(tts): add Gemini persona prompt file` — `hermes_cli/config.py`（+11） / `tests/tools/test_tts_gemini.py`（+57）
- `2c19208 feat(tts): add Gemini audio tag rewrite` — `cli-config.yaml.example`（+25） / `hermes_cli/config.py`（+12）

### 11.3 Langfuse base64 data URI redact

`4642762 fix(langfuse): redact base64 data URIs instead of truncating into invalid base64`（kshitijk4poor）— Langfuse SDK 把 `data:*;base64,...` 当 media try decode；`_truncate_text` 在 payload 中部切片产生 invalid base64 + noisy *"Error parsing base64 data URI"*。修：observability 只需 metadata，整段 redact data URI 而非截。

### 11.4 MiniMax M3 1M 硬编码 over stale models.dev probe

`5a4297a fix(model_metadata): prefer hardcoded 1M for MiniMax M3 over stale models.dev probe`（达令小新）— `agent/model_metadata.py:1838-1849` 在 `get_model_context_length()`：

```python
if ctx:
    # MiniMax M3: models.dev reports 512K but actual context is 1M.
    # Prefer hardcoded catalog over stale probe value.
    if _model_name_suggests_minimax_m3(model):
        catalog = DEFAULT_CONTEXT_LENGTHS.get("minimax-m3")
        if catalog and ctx < catalog:
            logger.info(
                "Rejecting models.dev context=%s for %r "
                "(MiniMax-M3 underreport); using hardcoded default %s",
                ctx, model, f"{catalog:,}",
            )
            ctx = catalog
    return ctx
```

### 11.5 xAI Grok Build code 粘贴 + 8x8 screenshot guard

`1c055a4 fix(xai): accept Grok Build code during loopback wait + tiny screenshot guard`（Ondrej Drapalik）— xAI consent page 在页面内 render authorization code 不 redirect 到 loopback callback；监听器只能 hang 到 timeout，manual-paste flow 要 callback URL（不含 token）：

- `hermes_cli/auth.py:2665+` — `_xai_wait_for_callback` 加 `manual_paste_redirect_uri` 参数；poll stdin non-blockingly while waiting；接受 pasted bare Grok Build code，本地 substitute state（PKCE code_verifier 仍 bind exchange）。无需 timeout 或 `--manual-paste` 重跑
- `computer_use`：从 base64 解 PNG/JPEG 尺寸；小于 provider min（8x8）—xAI 直接 HTTP 400 — fall back 到 text/AX/SOM payload
- `model_setup_flows.py`：xAI credential reuse prompt 用 shared `_prompt_auth_credentials_choice` radio picker
- `main.py`：`_prompt_provider_choice` 传 title；helper import 重新 home（flows 已抽出到 `model_setup_flows.py`）

Salvaged from #36781 onto current main。

### 11.6 Curator 共享 atomic state writer

`47e77ae fix(curator): use shared atomic state writer`（Gille）— `agent/curator.py:save_state()` 之前自己写 13 行 `tempfile.mkstemp + fdopen + flush + fsync + os.replace + cleanup-on-exception`；改用 `utils.atomic_json_write(path, data, indent=2, sort_keys=True)` 一行：

```python
-    fd, tmp = tempfile.mkstemp(dir=str(path.parent), prefix=".curator_state_", suffix=".tmp")
-    try:
-        with os.fdopen(fd, "w", encoding="utf-8") as f:
-            json.dump(data, f, indent=2, sort_keys=True, ensure_ascii=False)
-            f.flush()
-            os.fsync(f.fileno())
-        os.replace(tmp, path)
-    except BaseException:
-        try:
-            os.unlink(tmp)
-        except OSError:
-            pass
-        raise
+    atomic_json_write(path, data, indent=2, sort_keys=True)
```

`-17 / +4` 净缩。

### 11.7 Desktop 隐藏 Windows console children

`4c797d0 fix(desktop): hide Windows console children launched by GUI`（Gille）— `apps/desktop/electron/bootstrap-runner.cjs`（+13） / `main.cjs`（+37/-18） / 新 `windows-child-process.test.cjs`（+54 行）/ `package.json`（+2/-2）。

### 11.8 `.env` 文件模式保留

- `1581333 fix(config): preserve original .env file mode in remove_env_value too (#43349)` — `remove_env_value` 也保留原 mode
- `e4a1b35 fix(config): preserve original .env file mode instead of unconditionally tightening to 0600 (#33699)` — 写 `.env` 不再无条件收紧 0600

### 11.9 Self-heal 半建 venv

`a5c32cd fix(update): self-heal a venv left half-built by an interrupted install (#42172)`（Teknium）— update 在 dep install 中途被杀（Ctrl-C / 终端关闭 / WSL OOM）可能让 venv 失去 pip 与核心 deps（如 Pillow）。Drop install-scoped `.update-incomplete` breadcrumb 在 dep install 之前、在核心-dependency verification pass 之后才清除。后续启动检测 breadcrumb 时 ensurepip + 重装。

### 11.10 Suppress *"Credit access paused"* on free models

`6110aed Suppress "Credit access paused" notice on free models (#43669)`（rob-maron）— `agent/credits_tracker.py`（+85/-19） / `run_agent.py`（+11/-3） / `tests/agent/test_credits_cold_start.py`（+23）— 免费模型不显示 credit 暂停提示。

### 11.11 Desktop 模型 runtime state per session

`6de3963 fix(desktop): keep model runtime state per session (#43702)`（brooklyn!）— 三次 cherry-pick 整合：
- `f72ee87d` keep model runtime state per session
- `d91942eb` keep footer model state scoped to active session
- 第三 commit：restore stored runtime when resuming sessions

### 11.12 Memory `@file:` 二进制引用现可操作

`7ffc216 fix(agent): make a binary @file: reference actionable instead of a dead end`（Brooklyn）— 之前 binary `@file:`（PDF、docx、spreadsheet）展开成 *"binary files are not supported"* bare warning 无内容、模型见 failure 即 give up（如 dropped PDF 回 *"unsupported"* text note 尽管文件就在旁边）。修：注入 actionable content block — 路径、mime type、size、+ 提示用 tools read/convert/view（且明确 *不要* 告诉用户类型 unsupported）。General across every binary type — 非 PDF-specific。

### 11.13 CI exit-4 forensics

- `f082b4e fix(ci): make parallel runner's exit-4 retry robust for newly-added test files (#42994)`
- `07ac185 fix(ci): exit-4 forensics for vanishing test files in run_tests_parallel.py (#43646)`

每文件 test runner 在 pytest exit 4（*"file or directory not found"*）但 file on disk 时 retry 一次；exit-4 耗尽 retries 时 append filesystem forensics。

### 11.14 工具链三连

- `3bfbb3f change(tooling): typecheck in CI, update ts to 6`（ethernet）— CI 加 typecheck、TS 升 6 + 修 ui-tui 真类型错误
- `90f4b30 change(tooling): remove react-compiler eslint, update concurrently` — `concurrently 9` 有 critical vuln dep；`react-compiler` eslint plugin 已并入 `react-hooks` eslint plugin
- `4cecb1a change(tooling): npm audit fix in website/` — `website/package-lock.json` audit fix（-37/+37 行）

### 11.15 AGENTS.md Design Philosophy + Contribution Rubric

`6b33052 docs(agents): add Design Philosophy + Contribution Rubric to AGENTS.md (#42641)`（Teknium）— AGENTS.md 几乎全是 how-to/mechanics、`want/don't-want` guidance 隐含且零散。加单一权威 intent 层在顶部，校准至 *实际 merge 的* 与 *实际拒绝的*：

- **What Hermes Is** + 两核心 properties — *"Per-conversation prompt caching is sacred"* + *"The core is a narrow waist; capability lives at the edges"*
- **Contribution Rubric — What We Want / What We Don't**：明示 sweeper 三 close 原因（`implemented_on_main` / `cannot_reproduce` / `incoherent`）；taste-based *"out of scope"* 留给 human maintainer
- **What we want**：fix real bugs well / expand reach at the edges / refactor god-files / keep core narrow / extend don't duplicate / behavior contracts over snapshots / E2E validation / cache-/alternation-/invariant-safe / contributor credit preserved
- **What we don't want**：speculative infrastructure / new `HERMES_*` env vars for non-secret config（`.env` 仅限 secrets，所有 behavioral settings 进 `config.yaml`） / new core tools when terminal+file suffice / lazy-reading escape hatches / "fixes" that destroy the feature / outbound telemetry without opt-in
- 新增 *"Footprint Ladder"* — extend existing → CLI+skill → service-gated tool（`check_fn`） → plugin → MCP server → new core tool（last resort）

### 11.16 TUI session-name 在 terminal titlebar

`d339653 feat(tui): include session name in the terminal titlebar (#43188)` — 见 §3.8 完整 file:line（`ui-tui/src/domain/paths.ts:27-40` `composeTabTitle()` + `ui-tui/src/app/useMainApp.ts:567` 集成）。

### 11.17 Telegram QR onboarding 流（见 §6.4）

### 11.18 其他小修

- `383d44b fix(web): rank explicit credentials above managed-gateway probe` — 重排 web-search backend selection 优先 direct-credential（tavily / exa / parallel）over managed-gateway firecrawl probe，让有显式 credentials 的用户拿到配置的 backend
- `af978ec fix(model): require confirmation for expensive model selections` — 在 `hermes_cli/model_setup_flows.py` / Discord adapter / web dashboard 全套 picker 流上 wire expensive-model confirmation gate，带 slash-confirm 按钮 / 文本 fallback，切高费用模型前需用户确认
- `243cada fix(model): cover typed gateway /model path + async-safe pricing lookups` — `gateway/slash_commands.py` 让 typed `/model <name>` 经 expensive-model confirmation gate（不再 bypass）；`hermes_cli/web_server.py` 与平台 pricing-lookup 路径把 `expensive_model_warning()` 包 `asyncio.to_thread()` — I/O 密集的查询不 block event loop
- `57c6714 fix(models): keep curated Anthropic aliases in /model picker (#43103)` — `agent/model_picker.py` merge 手工 `_PROVIDER_MODELS["anthropic"]` 与 live `/v1/models` catalog（curated 先 + live-only 后 + dedup），让如 `claude-fable-5` 等新 alias 在 models endpoint 枚举之前也能在 picker 里出现
